data:
  sample_rate: 44100
  mono: 1
  batch_size: 8
  n_workers: 16

experiment:
  learning_rate: 0.001
  max_epochs: 200
  n_label: 15

network:
  n_fft: 2048
  n_mels: 40
  f_min: 0
  f_max: 22050

training:
  max_epochs: 200
  strategy: ddp
  accelerator: gpu
  devices: 1

logging:
  tb_log_dir: "~/dcase/tb_logs/"
  ckpt_dir: "~/dcase/ckpts/"